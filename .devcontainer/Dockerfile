# 使用一个由微软官方提供、预装了 Java 17 及常用开发工具的镜像
# 这个镜像已经内置了 JDK 17, Maven, Git, Node.js 等常用工具，非常适合作为开发环境
FROM mcr.microsoft.com/devcontainers/java:1-17

# devcontainer.json 文件已经指定了工作目录为 /workspace，
# 所以这里的 WORKDIR 设置不是必须的，但它是一个好的实践。
WORKDIR /workspace
