# 媒体资源分离技术实现指南

## 一、核心概念

### 1.1 预签名URL上传流程
MinIO 预签名URL上传是一种安全的文件上传方式，流程如下：

```
客户端 → cloud-web → Feign → cloud-Media → MinIO
   ↓         ↓         ↓         ↓         ↓
请求上传  获取URL   返回URL   生成URL   验证URL
   ↓         ↓         ↓         ↓         ↓
上传文件  使用URL   上传到MinIO  验证文件  存储文件
   ↓         ↓         ↓         ↓         ↓
完成上传  通知完成  更新状态  记录资产  返回结果
```

### 1.2 分片上传流程（大文件）
对于大文件（>5MB），使用分片上传：

```
1. 初始化分片上传 → 获取 uploadId 和分片URL列表
2. 并行上传分片   → 每个分片独立上传
3. 完成分片上传   → 合并所有分片
```

---

## 二、关键代码实现

### 2.1 预签名URL上传工具类

```java
package com.overthinker.cloud.web.utils;

import okhttp3.*;
import org.springframework.stereotype.Component;
import lombok.extern.slf4j.Slf4j;

import java.io.IOException;

/**
 * MinIO 预签名URL上传工具
 */
@Slf4j
@Component
public class MinioPresignedUploadHelper {

    private final OkHttpClient client = new OkHttpClient();

    /**
     * 使用预签名URL上传文件到MinIO
     *
     * @param presignedUrl 预签名URL
     * @param file         文件
     */
    public void uploadWithPresignedUrl(String presignedUrl, MultipartFile file) {
        try {
            // 构建请求体
            RequestBody requestBody = RequestBody.create(
                file.getBytes(),
                MediaType.parse(file.getContentType())
            );

            // 构建HTTP请求
            Request request = new Request.Builder()
                .url(presignedUrl)
                .put(requestBody)
                .build();

            // 执行上传
            Response response = client.newCall(request).execute();

            if (!response.isSuccessful()) {
                throw new RuntimeException("上传失败: HTTP " + response.code());
            }

            log.info("文件上传成功: {}", file.getOriginalFilename());

        } catch (IOException e) {
            log.error("使用预签名URL上传失败", e);
            throw new RuntimeException("上传失败: " + e.getMessage(), e);
        }
    }

    /**
     * 分片上传（用于大文件）
     *
     * @param partUrls 分片URL列表
     * @param file     文件
     */
    public void uploadParts(Map<String, String> partUrls, MultipartFile file) {
        // 实现分片上传逻辑
        // 1. 将文件分割成多个分片
        // 2. 使用对应的URL上传每个分片
        // 3. 记录每个分片的ETag
    }
}
```

### 2.2 URL解析工具类

```java
package com.overthinker.cloud.web.utils;

import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Component;

/**
 * 媒体URL解析工具
 */
@Component
public class MediaUrlUtils {

    @Value("${minio.bucketName}")
    private String bucketName;

    /**
     * 从完整URL中提取objectName
     *
     * @param fullUrl 完整URL
     * @return objectName
     */
    public String extractObjectName(String fullUrl) {
        int bucketIndex = fullUrl.indexOf(bucketName);
        if (bucketIndex == -1) {
            throw new IllegalArgumentException("URL中未找到bucketName");
        }

        // 提取 bucketName 之后的部分
        return fullUrl.substring(bucketIndex + bucketName.length() + 1);
    }

    /**
     * 从URL中提取文件名
     *
     * @param url 完整URL
     * @return 文件名
     */
    public String extractFileName(String url) {
        int lastSlash = url.lastIndexOf('/');
        if (lastSlash == -1) {
            return url;
        }
        return url.substring(lastSlash + 1);
    }
}
```

---

## 三、Service迁移示例

### 3.1 ArticleServiceImpl 迁移示例

#### 原实现（直接MinIO）
```java
@Override
public ResultData<String> uploadArticleCover(MultipartFile articleCover) {
    try {
        String articleCoverUrl = fileUploadUtils.uploadImage(UploadEnum.ARTICLE_COVER, articleCover);
        if (MyStringUtils.isNotNull(articleCoverUrl)) {
            return ResultData.success(articleCoverUrl);
        } else {
            return ResultData.failure("上传格式错误");
        }
    } catch (FileUploadException e) {
        return ResultData.failure(e.getMessage());
    } catch (Exception e) {
        log.error("文章封面上传失败", e);
        return ResultData.failure("文章封面上传失败");
    }
}
```

#### 新实现（Feign调用）
```java
@Override
public ResultData<String> uploadArticleCover(MultipartFile articleCover) {
    try {
        // 1. 构建上传请求
        PresignedUploadRequest request = new PresignedUploadRequest(
            articleCover.getOriginalFilename(),
            articleCover.getSize(),
            articleCover.getContentType(),
            null,  // MD5可选
            "ARTICLE_COVER"  // 文件类型标识
        );

        // 2. 获取预签名URL
        Long userId = SecurityUtils.getUserId();
        ResultData<Map<String, Object>> presignResult = mediaClient.getPresignedUploadUrl(userId, request);

        if (!presignResult.isSuccess()) {
            return ResultData.failure("获取上传URL失败");
        }

        Map<String, Object> data = presignResult.getData();
        String presignedUrl = (String) data.get("presignedUrl");
        String objectName = (String) data.get("objectName");

        // 3. 使用预签名URL上传文件到MinIO
        minioPresignedUploadHelper.uploadWithPresignedUrl(presignedUrl, articleCover);

        // 4. 完成上传
        ResultData<Long> completeResult = mediaClient.completeUpload(objectName);
        if (!completeResult.isSuccess()) {
            return ResultData.failure("完成上传失败");
        }

        // 5. 获取文件访问URL
        ResultData<String> urlResult = mediaClient.getFileUrl(objectName);
        return urlResult;

    } catch (Exception e) {
        log.error("文章封面上传失败", e);
        return ResultData.failure("文章封面上传失败: " + e.getMessage());
    }
}
```

#### 删除方法迁移
```java
@Override
public ResultData<Void> deleteArticleCover(String articleCoverUrl) {
    try {
        // 从URL中提取objectName
        String objectName = mediaUrlUtils.extractObjectName(articleCoverUrl);

        // 调用Feign删除文件
        Long userId = SecurityUtils.getUserId();
        ResultData<Void> deleteResult = mediaClient.deleteFile(userId, objectName);

        return deleteResult;

    } catch (Exception e) {
        log.error("删除文章封面失败", e);
        return ResultData.failure();
    }
}
```

---

### 3.2 VideoServiceImpl 迁移示例（分片上传）

```java
@Override
public Map<String, Object> uploadVideo(MultipartFile videoFile) {
    try {
        // 1. 参数校验
        validateVideoFile(videoFile);

        // 2. 初始化分片上传
        InitiateMultipartUploadRequest request = new InitiateMultipartUploadRequest(
            videoFile.getOriginalFilename(),
            videoFile.getSize(),
            videoFile.getContentType(),
            null,
            "VIDEO"
        );

        Long userId = SecurityUtils.getUserId();
        ResultData<Map<String, Object>> initiateResult = mediaClient.initiateMultipartUpload(userId, request);

        if (!initiateResult.isSuccess()) {
            throw new RuntimeException("初始化分片上传失败");
        }

        Map<String, Object> data = initiateResult.getData();
        String uploadId = (String) data.get("uploadId");
        Map<String, String> partUrls = (Map<String, String>) data.get("partUrls");

        // 3. 分片上传
        minioPresignedUploadHelper.uploadParts(partUrls, videoFile);

        // 4. 完成分片上传
        ResultData<Long> completeResult = mediaClient.completeMultipartUpload(uploadId);

        // 5. 获取视频URL
        String objectName = (String) data.get("objectName");
        ResultData<String> urlResult = mediaClient.getFileUrl(objectName);

        return Map.of(
            "video", urlResult.getData(),
            "videoSize", convertVideoSize(videoFile.getSize()),
            "videoType", videoFile.getContentType()
        );

    } catch (Exception e) {
        log.error("视频上传失败", e);
        throw new RuntimeException("视频上传失败", e);
    }
}
```

---

## 四、配置与依赖

### 4.1 cloud-web pom.xml 依赖调整

```xml
<dependencies>
    <!-- 移除 MinIO 直接依赖 -->
    <!--
    <dependency>
        <groupId>io.minio</groupId>
        <artifactId>minio</artifactId>
    </dependency>
    -->

    <!-- 添加 Feign 客户端依赖 -->
    <dependency>
        <groupId>com.overthinker.cloud</groupId>
        <artifactId>cloud-api</artifactId>
    </dependency>

    <!-- 保留 OkHttp 用于预签名URL上传 -->
    <dependency>
        <groupId>com.squareup.okhttp3</groupId>
        <artifactId>okhttp</artifactId>
    </dependency>
</dependencies>
```

### 4.2 Feign 客户端注入

```java
@Service
@RequiredArgsConstructor
public class ArticleServiceImpl ... {

    // 移除原有的上传工具
    // private final FileUploadUtils fileUploadUtils;

    // 注入新的 Feign 客户端
    private final MediaClient mediaClient;
    private final MinioPresignedUploadHelper minioPresignedUploadHelper;
    private final MediaUrlUtils mediaUrlUtils;
}
```

---

## 五、错误处理

### 5.1 统一异常处理

```java
@Component
public class MediaUploadExceptionHandler {

    /**
     * 处理上传异常
     */
    public ResultData<String> handleUploadException(Exception e, String operation) {
        log.error("{}失败", operation, e);

        if (e instanceof FeignException) {
            return ResultData.failure("媒体服务调用失败: " + e.getMessage());
        } else if (e instanceof RuntimeException) {
            return ResultData.failure("上传失败: " + e.getMessage());
        } else {
            return ResultData.failure("系统错误");
        }
    }
}
```

### 5.2 重试机制

```java
@Retryable(
    value = {FeignException.class},
    maxAttempts = 3,
    backoff = @Backoff(delay = 1000, multiplier = 2)
)
public ResultData<Map<String, Object>> getPresignedUploadUrl(Long userId, PresignedUploadRequest request) {
    return mediaClient.getPresignedUploadUrl(userId, request);
}
```

---

## 六、测试示例

### 6.1 单元测试

```java
@SpringBootTest
class ArticleServiceImplTest {

    @MockBean
    private MediaClient mediaClient;

    @Autowired
    private ArticleService articleService;

    @Test
    void testUploadArticleCover() {
        // Mock Feign响应
        Map<String, Object> mockData = Map.of(
            "presignedUrl", "http://minio.example.com/presigned",
            "objectName", "article/cover/test.jpg"
        );
        when(mediaClient.getPresignedUploadUrl(anyLong(), any()))
            .thenReturn(ResultData.success(mockData));

        when(mediaClient.completeUpload(anyString()))
            .thenReturn(ResultData.success(1L));

        when(mediaClient.getFileUrl(anyString()))
            .thenReturn(ResultData.success("http://minio.example.com/article/cover/test.jpg"));

        // 执行测试
        MultipartFile file = createMockFile();
        ResultData<String> result = articleService.uploadArticleCover(file);

        // 验证结果
        assertTrue(result.isSuccess());
        assertNotNull(result.getData());
    }
}
```

---

## 七、性能优化建议

### 7.1 异步上传
```java
@Async
public CompletableFuture<ResultData<String>> uploadArticleCoverAsync(MultipartFile file) {
    return CompletableFuture.completedFuture(uploadArticleCover(file));
}
```

### 7.2 批量上传
```java
public ResultData<List<String>> uploadMultipleFiles(List<MultipartFile> files) {
    List<CompletableFuture<ResultData<String>>> futures = files.stream()
        .map(file -> CompletableFuture.supplyAsync(() -> uploadArticleCover(file)))
        .toList();

    List<String> urls = futures.stream()
        .map(CompletableFuture::join)
        .filter(ResultData::isSuccess)
        .map(ResultData::getData)
        .toList();

    return ResultData.success(urls);
}
```

---

## 八、监控与日志

### 8.1 上传监控
```java
@Aspect
@Component
public class MediaUploadMonitor {

    @Around("execution(* com.overthinker.cloud.web.service.impl.*ServiceImpl.upload*(..))")
    public Object monitorUpload(ProceedingJoinPoint pjp) throws Throwable {
        long startTime = System.currentTimeMillis();
        String methodName = pjp.getSignature().getName();

        try {
            Object result = pjp.proceed();
            long duration = System.currentTimeMillis() - startTime;

            log.info("上传成功: {} 耗时: {}ms", methodName, duration);

            // 记录到监控系统
            metricsService.recordUploadSuccess(methodName, duration);

            return result;

        } catch (Exception e) {
            log.error("上传失败: {}", methodName, e);
            metricsService.recordUploadFailure(methodName, e.getClass().getSimpleName());
            throw e;
        }
    }
}
```

---

## 九、常见问题FAQ

### Q1: 为什么使用预签名URL而不是直接上传？
**A**: 预签名URL上传有以下优势：
- 安全性：URL有时效限制，避免长期暴露
- 解耦：cloud-web 不需要 MinIO SDK
- 灵活性：可以在客户端直接上传，减少服务器负载

### Q2: 大文件上传如何处理？
**A**: 对于 >5MB 的文件：
- 使用分片上传
- 每个分片独立上传，失败可重试
- 支持断点续传

### Q3: 如何处理上传失败？
**A**: 实现三层保障：
1. Feign调用重试机制（3次）
2. 本地临时存储作为备份
3. 定时任务清理未完成的资产记录

### Q4: 性能会下降吗？
**A**: 合理优化后性能不会下降：
- 使用异步上传减少等待时间
- 预签名URL上传直接到MinIO，无中间转发
- 分片上传支持并行处理

---

## 十、参考资料

- [MinIO预签名URL文档](https://min.io/docs/minio/linux/developers/java/presigned-put-object-v2.html)
- [Spring Cloud OpenFeign文档](https://spring.io/projects/spring-cloud-openfeign)
- [OkHttp使用指南](https://square.github.io/okhttp/)

---

**版本**: v1.0
**创建时间**: 2026-06-20
**维护者**: 开发团队